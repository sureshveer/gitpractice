public class comparetwostrings {

    public static void task18(String a,String b){

        if(a.length()>b.length()){
            system.debug('a has more letters than b'+a +b);
        }
        else{

            system.debug('b has more letters than a'+a +b);
        }
    }
}